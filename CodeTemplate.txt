CODE TEMPLATE:
//This is a single line comment (notes that won't be read as instructions to the computer).
//Different syntax is used for comments depending on the language.

//CHANGE ME: Animal Crossing New Horizons
//CHANGE ME: Melanie Huynh


if (player shakes full fruit tree) {
    //fruit falls from tree;
} else {
    //nothing falls from tree;
}

if (player presses 'Y' button) && (player is standing near item) {
    //player picks up item to store in inventory;
} else {
    //player picks up no item;
}

if (player buys an item from store) {
    //subtract cost of item from bells, add item to inventory, remove item from store;
} else {
    //no bells are subtracted and item is left in store;
}


